interface:
  display_name: "AI Data Update Workflow"
  short_description: "Run safe data updates with source validation, scoped writes, read-back checks, and anomaly notes."
  default_prompt: "Design or execute a safe data update workflow with source checks, scoped writes, read-back verification, and anomaly reporting."
